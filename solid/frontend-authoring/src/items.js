export const baseItems = Object.freeze([
  Object.freeze({ label: 'alpha' }),
  Object.freeze({ label: 'beta' }),
]);

export const updatedItems = Object.freeze([
  Object.freeze({ label: 'beta' }),
  Object.freeze({ label: 'gamma' }),
  Object.freeze({ label: 'delta' }),
]);
